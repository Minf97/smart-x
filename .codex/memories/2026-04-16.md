# 项目创建进度和本地仓库路径

- 目标：修正创建项目的 repoId / repoName 使用，增加创建进度反馈，设置页支持本地仓库路径。
- 改动：
  - 创建项目改为 session + poll，进度步骤包括 `listProjectRows`、`validateProjectConnection`、`createBackendProject`、`cloneManagedRepo`、`saveProject`。
  - 设置页新增 `RepoPathField`，支持编辑本地仓库路径和打开目录。
  - 默认本地仓库路径当前为 `~/Documents/workspace/managed-repos/<provider>/<repo segments...>`。
  - clone / 更新仓库统一走 `ensureManagedRepo`，尽量复用已有目录。
- 验证：`biome check` 通过，`tsc --noEmit --skipLibCheck` 通过。
- 待办：后续可增加系统“选择文件夹”对话框。

# RepoPathField 去耦

- 目标：减少 `RepoPathField` 入参，不要高耦合 props。
- 改动：
  - `RepoPathField` 已改为零 props。
  - 组件内部直接用 `useCurrentProject()` 读取当前 project。
  - 设置页提交时通过 `FormData` 读取 `managedRepoPath`。
- 验证：`biome check` 通过，`tsc --noEmit --skipLibCheck` 通过。
- 待办：无。

# repository 拆分

- 目标：针对创建项目相关新增内容，降低 `repository.ts` 体积。
- 改动：
  - 路径逻辑拆到 `src/server/projects/repo-path-service.ts`。
  - 创建进度 session 拆到 `src/server/projects/project-create-progress.ts`。
  - `repository.ts` 只保留主要读写和业务流程。
- 验证：`biome check` 通过，`tsc --noEmit --skipLibCheck` 通过。
- 待办：如果继续变大，可再拆 create/update project 流程。

# repository 删除无意义 helper

- 目标：删掉用户认为没有意义的转换函数。
- 改动：
  - 删除 `toPublicProject`。
  - 删除 `buildStoredProject`。
  - 删除 `toAiConfig`。
  - 删除 `toProjectInput`。
  - 相关逻辑内联到调用位置。
- 验证：`biome check` 通过，`tsc --noEmit --skipLibCheck` 通过。
- 待办：继续按简单直接的风格调整后续代码。

# repository 简化和每日记忆规则

- 目标：参考用户手改的 `validateProjectConnection`，把项目创建/更新逻辑写得更直白，并调整 memory 规则。
- 改动：
  - 保留 `validateProjectConnection` 的直接变量流风格。
  - `createProject` 和 `runCreateProjectSession` 直接取 `repoConfig`、`connection`、`validated`，不再使用额外转换 helper。
  - `updateProject` 删除多余的 `nextInput` 清洗对象，直接使用 schema 处理后的 `input`。
  - `AGENTS.md` 改为每天一个 `.codex/memories/YYYY-MM-DD.md`，同一天多次任务用一级标题分隔。
  - 合并并删除旧的按任务拆分 memory 文件。
- 验证：`biome check src/server/alerts/repository.ts` 通过，`tsc --noEmit --skipLibCheck` 通过。
- 待办：无。

# 报警手动刷新和仓库下拉禁用态

- 目标：增加一个手动刷新报警列表的按钮，并让已连接过的 repo 在创建项目时不可再选。
- 改动：
  - `src/components/dashboard/sidebar-panel.tsx` 新增 `AlertsRefreshButton`，使用 `RefreshCw` 图标手动触发 `syncAlerts()`，并立即重新拉取报警列表。
  - `src/components/dashboard/create-project-dialog.tsx` 将仓库选择从原生 `select` 改成 shadcn `DropdownMenu`。
  - repo 列表会根据当前 `projects` 计算已连接仓库；已连接项展示 `已连接 / Already connected` badge，并处于禁用态。
  - 自动默认选择逻辑改为优先选择第一个“未连接”的 repo；如果全部都连过，则不再自动选中。
  - 新增 i18n 文案：刷新报警、刷新失败/成功、全部仓库已连接、已连接。
- 验证：`biome check src/components/dashboard/sidebar-panel.tsx src/components/dashboard/create-project-dialog.tsx src/localization/i18n.ts` 通过，`tsc --noEmit --skipLibCheck` 通过。
- 待办：如果之后仓库很多，可继续升级成带搜索的 combobox。

# 刷新容错和 repo dropdown 抽组件

- 目标：解决手动刷新时报 `Remote request failed`，并把创建项目里的 repo dropdown 抽成独立组件。
- 改动：
  - `src/server/alerts/repository.ts` 的 `syncRemoteAlerts()` 改成按项目容错；单个项目远端同步失败时不再让整个刷新请求失败。
  - `shared/types/alert.ts` 新增 `failedProjectCount`，前端可感知有多少项目同步失败。
  - `src/components/dashboard/sidebar-panel.tsx` 在刷新成功但有失败项目时，改为 warning toast：本地列表已刷新，但部分项目同步失败。
  - 新增 `src/components/dashboard/repo-select-dropdown.tsx`，把 repo dropdown 从 `create-project-dialog.tsx` 抽离出去。
  - `src/components/dashboard/create-project-dialog.tsx` 现在只负责计算 repo options 和选中逻辑。
  - 新增 i18n 文案 `refreshAlertsPartial`。
- 验证：`biome check src/components/dashboard/repo-select-dropdown.tsx src/components/dashboard/create-project-dialog.tsx src/components/dashboard/sidebar-panel.tsx src/server/alerts/repository.ts shared/types/alert.ts src/localization/i18n.ts` 通过，`tsc --noEmit --skipLibCheck` 通过。
- 待办：如果 repo 继续增多，可把独立 dropdown 进一步升级成可搜索版本。

# currentProject 仓库信息收口

- 目标：按用户要求，不在 `create-project-dialog.tsx` 里堆 `connectedRepoIds` / `connectedRepoNames`，改成从 zustand 当前项目侧获取仓库信息。
- 改动：
  - 用户已说明上次刷新失败只是因为服务没重启，相关服务端容错改动已回滚，不继续保留。
  - `src/hooks/use-projects.ts` 新增 `useCurrentProjectRepo(provider)`，直接从项目 store 派生当前项目的 repo 配置。
  - `src/components/dashboard/create-project-dialog.tsx` 删除本地 `connectedRepoIds` / `connectedRepoNames` 集合计算。
  - 当前 repo 禁用逻辑改为只根据 `currentProject.repoConfig.repoId` / `repoName` 判断。
  - `RepoSelectDropdown` 组件保留，`CreateProjectDialog` 只负责传当前 provider 下的 repo options。
- 验证：`biome check src/hooks/use-projects.ts src/components/dashboard/create-project-dialog.tsx src/components/dashboard/repo-select-dropdown.tsx` 通过，`tsc --noEmit --pretty false --skipLibCheck` 通过。
- 待办：如果后面你还是想按“所有已连接项目”禁用，而不是按 `currentProject`，那就再把这块收成 store selector。

# 删掉 provider 状态屎函数

- 目标：去掉 `getCurrentProviderState` 这种纯堆砌 helper，改回直白可读的写法。
- 改动：
  - `src/components/dashboard/create-project-dialog.tsx` 删除 `getCurrentProviderState`。
  - 当前平台状态改回简单的 `isGitlab` + 4 个直白变量：
    - `currentAuth`
    - `currentFlow`
    - `currentRepos`
    - `currentReposLoading`
  - 为了不让主函数继续涨复杂度，补了两个明确职责的小块：
    - `useGitlabDefaultFields`
    - `useProviderAuthEffects`
  - 同时把 GitLab 表单块抽成 `GitlabConnectionFields`，避免主函数里再塞条件 JSX。
- 验证：`biome check src/components/dashboard/create-project-dialog.tsx src/hooks/use-projects.ts src/components/dashboard/repo-select-dropdown.tsx` 通过，`tsc --noEmit --pretty false --skipLibCheck` 通过。
- 待办：如果你还嫌 `create-project-dialog.tsx` 胖，下一步继续把 create 相关 mutation/query 收成独立 hook。

# settings dialog 限高滚动

- 目标：设置弹窗内容过长时限制高度，并允许自动滚动。
- 改动：
  - `src/components/dashboard/settings-dialog.tsx` 的 `DialogContent` 增加 `max-h-[85vh] overflow-y-auto`。
- 验证：`biome check src/components/dashboard/settings-dialog.tsx` 通过，`tsc --noEmit --pretty false --skipLibCheck` 通过。
- 待办：无。
