# 主链路闭环文档

## 目标

- 梳理 AI Alert Platform 与 User Project 之间的端到端闭环。
- 明确当前已通能力、主链路断点、状态闭环和后续实施切片。
- 用词表锁定“平台 / 用户项目 / 闭环”等易混术语。

## 改动

- `CONTEXT.md`
  - 新增领域词表，定义 AI Alert Platform、Remote Backend、Desktop Agent、User Project、Managed Repository、Alert、Alert Group、Analysis、Code Request、Feedback Signal。
- `docs/main-flow.md`
  - 新增主链路闭环方案，包含目标链路、当前事实、平台边界、实施切片、状态闭环、数据闭环、验收标准和下一步代码入口。

## 验证

- `npm run check`

## 待办

- 飞书 wiki 链接当前无法读取，后续拿到可访问内容后再逐条对齐。

# 抽出报警状态下拉组件

## 目标

- 将 sidebar 里的报警状态下拉按钮拆成可复用业务组件。
- 降低 `sidebar-panel.tsx` 与状态更新逻辑的耦合。

## 改动

- `src/components/dashboard/alert-status-dropdown.tsx`
  - 新增复用组件，内部负责状态菜单、状态更新、toast 和 alerts query cache 同步。
- `src/components/dashboard/sidebar-panel.tsx`
  - 移除状态更新细节，只保留列表布局和选中逻辑。

## 验证

- `./node_modules/.bin/tsc --noEmit --skipLibCheck`
- `npm test`
- `npm run check`

## 待办

- 无。

# 细化 Code Request 创建流程

## 目标

- 在主链路文档中细化 Code Request 从 base branch 到 PR/MR 的创建顺序。
- 明确创建 PR/MR 成功后 Alert 应进入 `in_review`。

## 改动

- `docs/main-flow.md`
  - 新增 Code Request 创建流程章节，覆盖切 base branch、pull、创建分支、定位、修复、提交、push、创建 PR/MR、状态更新。
  - 记录当前代码已覆盖部分和缺口：创建成功未自动 `in_review`、合并成功未自动 `done`、缺少创建进度状态。

## 验证

- `npm run check`

## 待办

- 后续实现 `createAlertRequest -> in_review` 和 `mergeAlertRequest -> done` 的自动状态流转。
