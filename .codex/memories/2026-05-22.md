# 处理记录真实数据

- 目标：移除处理记录模块里的 mock 时间线。
- 改动：`DetailActivity` 改为从报警创建时间、PR/MR 创建时间、`feedbackSignals` 生成记录，并按真实时间倒序展示；补充中英文文案和单元测试。
- 验证：`npm test` 通过；本次 touched 文件 `npx ultracite check ...` 通过。
- 待办：若后续需要展示“AI 分析开始/完成”的准确时间，需要后端单独持久化分析事件。

# 处理记录时间线美化

- 目标：让处理记录时间线更像组件化日志视图。
- 改动：参考 Magic UI / shadcn / Fumadocs 后选择 shadcn 风格组合；使用 lucide 图标、轨道线、事件卡片和 `Badge` 区分报警、PR/MR、反馈。
- 验证：`npm test` 通过；本次 touched 文件 `npx ultracite check ...` 通过。
- 待办：如需更复杂动画，可后续再评估引入 Magic UI 组件。

# 线上后端地址排查

- 目标：排查 `ALERTS_BACKEND_BASE_URL=https://smart-x-theta.vercel.app` 不可用原因。
- 改动：未改业务代码；确认桌面端会拼接 `/projects`、`/projects/:id/alerts`，后端 webhook 路由为 `/ingest/:webhookId`。
- 验证：`curl` 访问线上 `/health`、`/projects`、`/ingest/...` 均在连接 443 时超时。
- 待办：检查 Vercel 域名可达性、后端项目 Root Directory、`DATABASE_URL` 与 `BACKEND_BASE_URL` 环境变量。

# Vercel 控制台排查

- 目标：进入 Vercel 控制台确认 `smart-x` 后端部署状态。
- 改动：未改业务代码；用浏览器会话确认项目 Ready、Root Directory 为 `backend`、Framework 为 Hono、`DATABASE_URL` 存在。
- 验证：浏览器访问 `/health` 返回 `ok:true`；浏览器 fetch POST 旧 webhook 成功写入测试报警；Node fetch 与终端 curl 访问同域名超时。
- 待办：给 Vercel 补 `BACKEND_BASE_URL=https://smart-x-theta.vercel.app`；检查本机终端/Electron 是否未走浏览器代理。

# 本地代理连通修复

- 目标：解决本地 Node/Electron 请求线上 Vercel 超时。
- 改动：未改业务代码；确认系统代理为 `127.0.0.1:7892`，终端环境未设置代理变量。
- 验证：`NODE_USE_ENV_PROXY=1` 加 `HTTP_PROXY/HTTPS_PROXY` 后 Node fetch 成功；`verify:webhook --ack` 完整通过。
- 待办：本地启动 Electron 时带上代理环境变量，或后续做成专用 npm script。

# 本地代理 SOP 文档

- 目标：把浏览器可访问但 Node/Electron/终端超时的排查经验沉淀为 SOP。
- 改动：新增 `docs/local-proxy-sop.md`，补充本质解释、诊断命令、代理启动方式和项目验证脚本；在 README 与后端联调文档加入入口。
- 验证：人工检查文档结构和行数。
- 待办：如需更顺手，可后续新增带代理的 npm 启动脚本。

# 新增代理启动脚本

- 目标：把 Electron 代理启动命令收进 npm script。
- 改动：`package.json` 新增 `start:proxy`，设置 `NODE_USE_ENV_PROXY`、`HTTPS_PROXY`、`HTTP_PROXY` 后执行 `npm start`。
- 验证：用 Node 解析 `package.json` 成功。
- 待办：本地需要代理访问 Vercel 时使用 `npm run start:proxy`。
