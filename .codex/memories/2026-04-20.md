# 修复 pi 启动/重启报错

- 目标：修复接入 `pi` 后，Electron 主进程在启动或 `rs` 重启时抛出 `ERR_INVALID_ARG_TYPE`。
- 改动：`src/server/alerts/pi-service.ts` 改成运行时动态导入 `pi` SDK，并给 agent 目录与 `repoPath` 加兜底校验；`vite.main.config.mts` 将 `@mariozechner/pi-coding-agent` 设为 external。
- 验证：`./node_modules/.bin/tsc --noEmit --pretty false --skipLibCheck`、`npm run start`、终端输入 `rs` 重启主进程均通过。
- 待办：继续接报警分析按钮和本地仓库上下文检索链路。

# 盘点当前代码改动

- 目标：梳理当前工作区里的 staged / unstaged 改动分别在做什么。
- 改动：确认主线是“报警分析”从候选定位升级到 AI 分析，并且又从直连 `ai-service` 继续切到只读 `pi-service` agent；同时补了默认 AI 配置、按钮禁用条件、`impact` 展示和 `pi` 依赖/打包设置。
- 验证：查看 `git status --short`、`git diff HEAD --stat`，并逐个阅读 `shared/types/project.ts`、`src/components/dashboard/analysis-action.tsx`、`src/components/detail-content/analysis.tsx`、`src/localization/i18n.ts`、`src/server/alerts/ai-service.ts`、`src/server/alerts/pi-service.ts`、`src/server/alerts/repository.ts`、`package.json`、`vite.main.config.mts`。
- 待办：清理 staged / unstaged 分叉；决定是保留直连 `ai-service` 方案，还是正式切到 `pi-service` 并删除未使用实现。
