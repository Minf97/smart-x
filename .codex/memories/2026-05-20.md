# 新手教程链路调整

- 目标：把 onboarding 改成小圆点节点链路，压缩新建项目弹窗字段，并说明项目级 AI Settings 与 webhook 使用。
- 改动：新增 `TutorialNode`，onboarding 节点改为“新建项目 -> AI Settings -> 生成 Webhook”；创建项目弹窗移除 project name/base branch 输入，继续从 repo 自动写入；repo 下拉项左侧新增 GitHub/GitLab logo；AI 设置补充项目级用量说明。
- 验证：`npm run test -- src/tests/unit/onboarding-layout.test.tsx src/tests/unit/create-project-dialog-layout.test.tsx` 通过；本次 touched 文件 `biome check` 通过。
- 待办：全仓 `npm run check` 仍有既有 lint 问题；`tsc --noEmit` 仍受 node_modules 类型依赖问题阻塞。

# 新手教程单步化

- 目标：把 onboarding 从多节点列表改成单步决策流，并补充 step 切换动效。
- 改动：新增 `StepCard`，页面同屏只渲染当前 step；点击新建项目立即进入 AI Settings，取消且未建项目则回到 Step 1；点击 AI Settings 后进入 Webhook step；移除小圆点节点视觉；新增 `smartx-step-enter` 过渡动画。
- 验证：`npm run test -- src/tests/unit/onboarding-layout.test.tsx src/tests/unit/create-project-dialog-layout.test.tsx` 通过；本次 touched 文件 `biome check` 通过。
- 待办：全仓 `npm run check` 和 `tsc --noEmit` 仍受既有问题阻塞。

# 新手教程去标题化

- 目标：onboarding 页面只显示当前一步 step，不显示页面级标题、说明、badge 或稍后设置入口。
- 改动：移除 onboarding 外层 header 和 skip 按钮，页面居中展示单个 `StepCard`。
- 验证：`npm run test -- src/tests/unit/onboarding-layout.test.tsx src/tests/unit/create-project-dialog-layout.test.tsx` 通过；本次 touched 文件 `biome check` 通过。
- 待办：全仓 `npm run check` 和 `tsc --noEmit` 仍受既有问题阻塞。

# Figma 设计稿调用试验

- 目标：确认当前会话能否调用 Figma，并生成一张设计稿试样。
- 改动：发现 Figma MCP 工具未暴露；新增本地 SVG 设计稿 `.codex/designs/smart-x-dashboard-concept.svg`。
- 验证：使用 Ruby REXML 解析 SVG 通过；文件 123 行。
- 待办：连接 Figma MCP 后可把该设计稿迁移到 Figma 文件。

# Step 样式优化

- 目标：让当前 onboarding step 更像单一决策页，降低设置面板感。
- 改动：`StepCard` 收窄为居中卡片，增加顶部进度条、图标边框、层级化标题说明，并把主操作放到卡片底部右侧。
- 验证：`npm run test -- src/tests/unit/onboarding-layout.test.tsx src/tests/unit/create-project-dialog-layout.test.tsx` 通过；本次 touched 文件 `biome check` 通过。
- 待办：全仓 `npm run check` 和 `tsc --noEmit` 仍受既有问题阻塞。

# 新建项目内嵌 Step

- 目标：点击新建项目后不再打开 Dialog，而是在 onboarding 页面里进入普通 step。
- 改动：新增 `CreateProjectInline` 复用创建项目流程；onboarding 增加 `createProject` step，Step 2 内嵌平台和 repo 表单；Dashboard 的 `CreateProjectDialog` 保持弹窗行为。
- 验证：`npm run test -- src/tests/unit/onboarding-layout.test.tsx src/tests/unit/create-project-dialog-layout.test.tsx` 通过；本次 touched 文件 `biome check` 通过。
- 待办：全仓 `npm run check` 和 `tsc --noEmit` 仍受既有问题阻塞。

# GitLab Client ID 提示

- 目标：解释 GitLab Client ID 的来源，降低自建 GitLab 授权配置门槛。
- 改动：在 GitLab Client ID label 后增加 `CircleHelp` tooltip；提示用户创建 GitLab OAuth application、复制 Application ID，并配置 redirect URI；根据 GitLab URL 生成可点击的 `${gitlab_url}/oauth/applications` 链接。
- 验证：`npm run test -- src/tests/unit/onboarding-layout.test.tsx src/tests/unit/create-project-dialog-layout.test.tsx` 通过；本次 touched 文件 `biome check` 通过。
- 待办：全仓 `npm run check` 和 `tsc --noEmit` 仍受既有问题阻塞。

# 创建项目 fetch failed 优化

- 目标：定位创建项目进度到 `cloneManagedRepo` 后显示裸 `fetch failed` 的问题。
- 改动：创建进度 UI 只显示当前步骤文案，不暴露内部 step key；后端项目、GitHub、GitLab 请求增加 12 秒超时和明确错误来源；本地托管仓库准备失败统一提示 `Failed to prepare managed repository`。
- 验证：`npm run test -- src/tests/unit/onboarding-layout.test.tsx src/tests/unit/create-project-dialog-layout.test.tsx src/tests/unit/project-network-errors.test.ts` 通过；本次 touched 文件 `biome check` 通过。
- 待办：若仍失败，根据新错误文案区分检查 `ALERTS_BACKEND_BASE_URL`、GitHub/GitLab 网络或本地 git clone 权限。
