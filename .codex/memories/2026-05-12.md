# 分析报警前同步仓库

## 目标

- 点击“分析报警”后，先更新目标本地仓库，再读取代码上下文。
- 用 `git pull --rebase=false` 明确关闭 rebase。
- 补充分析报警流程文档。

## 改动

- `src/server/projects/git-service.ts`
  - 新增 `syncManagedRepoBaseBranch`，会更新 origin、检查工作区、同步基线分支。
  - 统一基线分支拉取为 `git pull --rebase=false origin <baseBranch>`。
- `src/server/alerts/repository.ts`
  - `analyzeAlert` 在本地定位代码前先同步项目仓库基线分支。
- `docs/analyze-alerts.md`
  - 新增点击分析后的入口、主流程、失败行为和相关文件说明。

## 验证

- `./node_modules/.bin/tsc --noEmit --skipLibCheck`
- `npm test`
- `npm run check`

## 待办

- 无。
