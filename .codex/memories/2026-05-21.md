# 后端接口 curl 测试

- 目标：验证 `Creating remote project` 卡住是否来自后端接口。
- 改动：未改代码；读取 `.env` 中 `ALERTS_BACKEND_BASE_URL=https://smart-x-theta.vercel.app`，用 curl 测 `/health` 和 `POST /projects`。
- 验证：远端 `/health` 与 `/projects` 均 12 秒超时；`curl -v` 显示域名解析到 `104.244.46.63` 后连接 443 超时；强制 `76.76.21.21` 可连 TCP 但 TLS 被 reset；本地 `8788` 和 `3001` 未监听目标后端。
- 待办：检查 Vercel 域名网络/DNS，或本地启动 backend 并把 `ALERTS_BACKEND_BASE_URL` 改成本地地址。

# 本地 backend 加载 env

- 目标：修复本地 backend 报 `Missing DATABASE_URL.`，但根目录 `.env` 已配置的问题。
- 改动：`backend/src/local-server.ts` 启动时读取仓库根目录 `.env`，仅补充当前进程未设置的变量，Vercel 仍使用平台环境变量。
- 验证：`backend` 类型检查通过；`biome check backend/src/local-server.ts` 通过；临时启动 `BACKEND_PORT=8790` 后 `/health` 返回 `databaseConfigured:true`，`POST /projects` 返回 201。
- 待办：现有 8788 进程需重启后才能加载新逻辑。

# 环境变量示例补全

- 目标：把本地和 Vercel 部署需要的环境变量集中放到 `.env.example`。
- 改动：补充 `ALERTS_BACKEND_BASE_URL`、`GITHUB_OAUTH_CLIENT_ID`、`GITLAB_OAUTH_CALLBACK_PORT`、`BACKEND_PORT`、`BACKEND_BASE_URL`、`DATABASE_URL`、`SMART_X_WEBHOOK_URL`、`BASE_URL` 示例值。
- 验证：`git diff --check .env.example` 通过；`.env.example` 被 Biome 配置忽略，未运行格式检查。
- 待办：线上 Vercel 需要单独配置 `DATABASE_URL` 和按域名设置 `BACKEND_BASE_URL`。

# Onboarding AI Settings 内嵌

- 目标：连接项目成功后进入内嵌 AI Settings，不打开任何 dialog，并优先使用环境变量。
- 改动：新增 `AiSettingsForm` 只渲染 Base URL、Model、API Key；onboarding 的 AI step 保存后进入 webhook；默认 AI 配置改为读取 `AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL`；`.env.example` 补充 AI 环境变量；本地 `.env` 写入用户提供的 AI 配置。
- 验证：`npm run test -- src/tests/unit/onboarding-layout.test.tsx src/tests/unit/create-project-dialog-layout.test.tsx` 通过；本次 touched 文件 `biome check` 通过；`npm --prefix backend run check` 通过；`git diff --check` 通过。
- 待办：真实应用进程需要重启后才能读取新的 `.env` AI 配置。

# 修复 process 未定义

- 目标：修复点击新建项目后 renderer 报 `process is not defined`。
- 改动：`getDefaultProjectAiConfig` 改为读取 `globalThis.__SMART_X_ENV__` 或安全的 `globalThis.process`；renderer Vite 配置注入 `AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL`、`SMART_X_WEBHOOK_URL`；`src/constants` 不再直接访问 `process`。
- 验证：`npm run test -- src/tests/unit/onboarding-layout.test.tsx src/tests/unit/create-project-dialog-layout.test.tsx` 通过；本次 touched 文件 `biome check` 通过；`npm --prefix backend run check` 通过；`git diff --check` 通过。
- 待办：需要重启 renderer dev server 才能应用新的 Vite define。

# 移除默认 AI 小函数

- 目标：按反馈去掉低价值小函数封装，避免 `getDefaultProjectAiConfig()` 这种三行逻辑函数。
- 改动：删除 `getDefaultProjectAiConfig`，改为导出 `DEFAULT_PROJECT_AI_CONFIG` 常量；调用方用对象展开拿副本；测试改为在模块导入前准备 AI 环境变量。
- 验证：`npm run test -- src/tests/unit/onboarding-layout.test.tsx src/tests/unit/create-project-dialog-layout.test.tsx` 通过；本次 touched 文件 `biome check` 通过；`npm --prefix backend run check` 通过；`git diff --check` 通过。
- 待办：后续新增小于 10 行逻辑时优先内联，除非是真正复用的公共工具。

# 收敛 AI 环境读取

- 目标：移除 `__SMART_X_ENV__` 和 `process.env` 兜底链，遵守不堆叠兜底逻辑。
- 改动：`DEFAULT_PROJECT_AI_CONFIG` 改回纯空值常量；`AiSettingsForm` 只通过 `import.meta.env.AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL` 读取前端环境变量；删除 renderer Vite 自定义全局注入；`src/constants` 改用 `import.meta.env.MODE`。
- 验证：`npm run test -- src/tests/unit/onboarding-layout.test.tsx src/tests/unit/create-project-dialog-layout.test.tsx` 通过；本次 touched 文件 `biome check` 通过；`npm --prefix backend run check` 通过；`git diff --check` 通过。
- 待办：renderer 需要重启后读取最新 `.env`。

# Webhook Step 进入 Dashboard

- 目标：补齐 onboarding 最后一步的收口，并允许用户在 Dashboard 随时回到接入指引。
- 改动：Webhook step 增加“进入 Dashboard”，点击后 `completeOnboarding()` 并跳转 `/dashboard`；已有项目重新打开 onboarding 时，AI 已配置则直接进入 webhook step，否则进入 AI Settings；Dashboard header 增加“接入指引”入口跳转 `/onboarding`。
- 验证：`npm run test -- src/tests/unit/onboarding-layout.test.tsx src/tests/unit/dashboard-header-layout.test.tsx src/tests/unit/create-project-dialog-layout.test.tsx` 通过；本次 touched 文件 `biome check` 通过；`npm --prefix backend run check` 通过；`git diff --check` 通过。
- 待办：无。

# 项目删除功能

- 目标：清理重复 connect 项目产生的项目记录和 Smart X 托管 clone。
- 改动：项目切换器增加“删除当前项目”确认弹窗；新增 `DELETE /projects/:id`、前端 API 和 store 删除动作；后端删除项目、关联 alerts、feedback signals，并且只删除 Smart X 管理目录下的本地 clone。
- 验证：`npm run test -- src/tests/unit/delete-project-service.test.ts src/tests/unit/project-switcher-delete.test.tsx src/tests/unit/onboarding-layout.test.tsx src/tests/unit/create-project-dialog-layout.test.tsx` 通过；本次 touched 文件 `biome check` 通过；`npm --prefix backend run check` 通过；`git diff --check` 通过。
- 待办：自定义仓库路径不会自动删除，避免误删用户工作目录。

# 分析与 PR 按钮禁用

- 目标：排查 Dashboard 里“分析报警”和“创建 PR/MR”按钮不可点击的原因。
- 改动：renderer Vite 配置增加 `envPrefix: ["VITE_", "AI_"]`，让 onboarding 的 AI Settings 能读取 `.env` 里的 AI 配置；项目 hydrate 时优先选择已有 `managedRepoPath` 的项目，避免重启后回到未配置的 mock 项目。
- 验证：读取本地 SQLite 发现 mock 项目缺本地仓库路径，真实项目 AI 配置为空；`npm run test` 通过。
- 待办：已有空 AI 配置的项目仍需重新保存一次 AI Settings，按钮才会按项目配置恢复可用。

# 自动模式按钮

- 目标：把右上角“重置新手流程”改成自动模式，开启后自动处理新报警。
- 改动：新增自动模式 store、旋转按钮和 Dashboard runner；自动模式开启后会找 `backlog/todo` 且项目 AI/仓库已配置的 Alert，依次执行分析、创建 PR/MR，并把本地缓存状态推进到 `in_review`。
- 验证：`npm run test` 通过；touched 文件 `ultracite check` 通过；`git diff --check` 通过。
- 待办：自动模式仅在当前桌面应用运行期间生效，暂未持久化开关状态。

# 报警分析 Prompt 精修

- 目标：让 AI 分析先判断真实业务/用户可见影响，再决定是否修复并创建 PR/MR。
- 改动：扩展 `Analysis` 增加 `businessImpact` 和 `fixDecision`；抽出共享分析/修复 prompt 与 schema；自动模式遇到 `keep_backlog` 会写回 Backlog 并跳过创建请求；创建请求入口也拒绝 `keep_backlog`。
- 验证：相关 Vitest、`npm run test`、touched 文件 Ultracite、`git diff --check` 通过；全量 `npm run check` 和 `npx tsc --noEmit` 仍被既有无关 lint/依赖类型问题阻塞。
- 待办：全量 Ultracite 和 TypeScript 项目检查需要单独清理仓库既有问题。
