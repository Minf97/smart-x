# AGENTS.md

本文件约束本仓库内 AI 代理的默认工作方式。若与用户当次明确指令冲突，以用户指令为准。

## 目标

- 保持实现最小、直接、可读。
- 优先交付稳定方案，不堆叠兜底逻辑。
- 让后续接手的人能快速看懂并继续改。
- 每次任务完成后，写一份压缩记忆到 `.codex/`。
- 同步更新 `.codex/latest-memory.md`。
- `.codex/memories/` 按天建文件：`YYYY-MM-DD.md`。
- 同一天的多次任务追加到当天文件里，用一级标题分隔。
- 不要为每次任务都新建一个 memory 文件。
- 记忆要简短，包含目标、改动、验证、待办。

## 核心规则

1. 函数和关键代码必须添加注释。
   注释长度控制在 4-10 个字，说明意图即可，不写废话。

2. 坚持最小实现。
   不做无明确收益的 fallback、兜底链路、兼容分支、预埋抽象。

3. 能用库就用库。
   优先使用 Shadcn UI 组件库、lucide 图标库等，不重复造轮子。

## 代码风格

1. 少抽象，少包装。
   不为“以后可能复用”提前拆层；只有在明显降低复杂度时才新增抽象。

2. 错误显式暴露。
   优先让错误可见、可定位，不吞错，不静默失败，不靠多重重试掩盖问题。

## 注释规则

1. 每个新增函数都要有一条简短注释。
2. 复杂条件、状态切换、异步流程、协议解析处必须补注释。
3. 注释解释“为什么”或“这段在做什么”，不要翻译代码字面意思。
4. 注释用中文，短句即可。

## 实现规则

1. 优先主路径。
   先把唯一清晰的主流程做对，再考虑扩展。

2. 不堆叠策略。
   同一问题不要同时塞入多套识别、回退、猜测、兼容方案。

3. 配置集中管理。
   常量、开关、可配置项集中放置，避免散落硬编码。

4. 修改要收敛。
   只改完成任务必需的文件和代码，不顺手重构无关区域。

## 验证规则



## 提交规则

1. 先读再改，不凭印象写代码。
2. 保持 diff 简洁，避免无关格式化。
3. 不主动重命名大文件、搬迁目录、替换技术栈。
4. 非用户要求，不引入“未来扩展”代码。

## 输出规则

1. 回答优先给结论，再给必要细节。
2. 解释代码时引用具体文件。
3. 若发现现有实现违反本文件，后续任务应优先朝本规范收敛。